# Changelog

Notable changes to the AgentWeb open core. Dates are UTC.

## Unreleased

- Added agent-discoverability artifacts: `SKILL.md` (agent-skill format),
  `CLAUDE.md`, `llms-full.txt` (full documentation in one file),
  `capabilities.json` (machine-readable reference-adapter index), `glama.json`
  and `server.json` (MCP directory metadata), and this changelog.
- README now leads with an agent-facing quick-connect section and MCP
  configuration.

## 0.19.3 — 2026-07

- Current public preview: CLI and Python runtime, login/session system, signed
  registry updater, four-tool MCP server (`agentweb mcp`), local analytics
  dashboard, and three open reference adapters (npm, arXiv, Wikipedia). See
  `README.md` for the full feature description.
